# hooks

App-wide hooks not tied to a single feature (e.g. use-media-query.ts for
desktop-centred vs mobile full-bleed layout). Feature-specific hooks live
inside their own features/<name>/hooks/ folder instead.
